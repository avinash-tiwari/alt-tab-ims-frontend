export default function OrdersPage() {
  return (
    <section className="page">
      <div className="sticky-header">
      <h2>Orders</h2>
      </div>
      <article className="card">
        <p>
          Orders CRUD, status management (PAID/UNPAID/OVERDUE), and printable PDF sharing can be
          wired once the orders API contract is available.
        </p>
      </article>
    </section>
  );
}
